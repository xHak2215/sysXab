from zipfile import ZipFile
import os
with ZipFile("Everything64.zip", "r") as myzip:
    myzip.extractall()
with ZipFile("gitHab.zip", "r") as myzip:
    myzip.extractall()
with ZipFile("Sublime Text 3.zip", "r") as myzip:
    myzip.extractall()
with ZipFile("oxi_joiner.zip ", "r",= hh) as myzip:
    myzip.extractall()
os.system('instalerprog.bat')
